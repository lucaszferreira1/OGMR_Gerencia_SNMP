Versão funcionando no Linux Ubuntu:

Primeiro instalar o node do seguinte link:
    nodejs.org

Instalar o snmp do Ubuntu:
    sudo apt install snmp

Instalar npm:
    npm install
Instalar as extensões:
    npm install express pg body-parser cors bcrypt jsonwebtoken dotenv react-router-dom@latest node-schedule

Rodar o arquivo, em um terminal, "server.js" com o seguinte comando:
    node server.js
Em um terminal diferente iniciar a página web:
    npm start